export class ProductEnt{
    constructor(data){
        productName=data.name,
        description=data.description,
        price=data.price,
        company=data.company,
        storage=data.storage,
        ram=data.ram
    }
}
